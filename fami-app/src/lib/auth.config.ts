// auth.config.ts — previously used by middleware.ts which has been replaced by proxy.ts.
// Kept as a no-op to avoid breaking any future imports; the real auth config lives in lib/auth.ts.
export const authConfig = {
  providers: [],
}
